plugins {
	id 'java'
}

dependencies {
	implementation project(':trevor-api')

	implementation "org.spongepowered:configurate-yaml:${configurateVersion}"

	implementation "redis.clients:jedis:${jedisVersion}"
}
